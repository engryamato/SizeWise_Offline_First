import React from 'react';
import { createRoot } from 'react-dom/client';

function App() {
  return <div>SizeWise — M0 Scaffold</div>;
}

createRoot(document.getElementById('root')!).render(<App />);

